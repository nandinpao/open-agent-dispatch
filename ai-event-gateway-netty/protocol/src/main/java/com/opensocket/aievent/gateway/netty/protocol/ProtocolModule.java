package com.opensocket.aievent.gateway.netty.protocol;

/** Compatibility marker for the protocol module; DTOs are owned by gateway-model. */
public final class ProtocolModule {
    private ProtocolModule() {
    }
}
