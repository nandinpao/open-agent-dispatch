apiVersion: v1
kind: Secret
metadata:
  name: opensocket-plugin-secret
  labels:
    app.kubernetes.io/name: openclaw-plugin-opensocket
type: Opaque
stringData:
  # Replace through an external secret controller or deployment pipeline.
  # Netty must cryptographically verify this JWT.
  agent.jwt: "replace-with-short-lived-signed-jwt"
