import {test,expect} from '@playwright/test';
test('Phase 2 release workspace exposes isolation and accessible governed action',async({page})=>{await page.route('**/api/a2a-cutover',route=>route.fulfill({contentType:'application/json',body:JSON.stringify({state:{stage:'LEGACY_WRITE_DISABLED',shadowReadEnabled:true,legacyWriteEnabled:false,issueTrackingEnabled:false,shadowMismatchCount:0,runtimeGateRunId:'run-p2i',releaseEvidenceReference:'evidence:pending-signature',version:5}})}));await page.goto('/a2a-operations/release');await expect(page.getByTestId('phase2-release-workspace')).toBeVisible();await expect(page.getByText('Issue Tracking').locator('..')).toContainText('Disabled');await expect(page.getByText('Legacy writes').locator('..')).toContainText('Disabled');await page.getByRole('button',{name:'Review governed advance'}).click();await expect(page.getByRole('dialog')).toBeVisible();await expect(page.getByLabel('Audit reason')).toBeFocused();await page.keyboard.press('Escape');await expect(page.getByRole('dialog')).toBeHidden();});
