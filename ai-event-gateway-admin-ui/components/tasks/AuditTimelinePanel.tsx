import React from "react";
export type AuditEvidenceRow={evidenceId:string;actorType:string;actorId:string;action:string;reasonCode?:string|null;auditReason?:string|null;outcome:string;correlationId:string;authorizationDecisionId?:string|null;occurredAt:string};
export function AuditTimelinePanel({rows=[]}:{rows?:AuditEvidenceRow[]}){return <section aria-labelledby="audit-timeline-heading"><h2 id="audit-timeline-heading">Audit Timeline</h2>{rows.length===0?<p>No audit evidence is available for this resource.</p>:<ol>{rows.map(row=><li key={row.evidenceId}><strong>{row.action}</strong><div>Actor: {row.actorType} / {row.actorId}</div><div>Outcome: {row.outcome}</div>{row.auditReason?<div>Reason: {row.auditReason}</div>:null}<div>Correlation ID: {row.correlationId}</div><time dateTime={row.occurredAt}>{row.occurredAt}</time></li>)}</ol>}</section>}
