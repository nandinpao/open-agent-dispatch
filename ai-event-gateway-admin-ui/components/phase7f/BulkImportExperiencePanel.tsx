import { bulkOperationPreview, importStageExperience, type ImportStage } from '@/lib/phase7f/operationsExportAttachmentUx';
const stages:ImportStage[]=['UPLOAD','PARSE','VALIDATE','DRY_RUN','AUTHORIZE','CONFLICT_PREVIEW','EXECUTE','RESULT'];
export function BulkImportExperiencePanel(){const preview=bulkOperationPreview({selected:100,eligible:82,denied:8,locked:2,stale:5,requiresApproval:3,policy:'PARTIAL_ALLOWED'});return <div className="space-y-5"><section className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-black text-slate-950">Bulk operation preview</h2><p className="mt-1 text-sm text-slate-600">The backend authorizes every resource. A rendered row capability or client selection is never sufficient.</p><dl className="mt-4 grid grid-cols-2 gap-3 md:grid-cols-6">{[['Selected',100],['Eligible',82],['Denied',8],['Locked',2],['Stale',5],['Approval',3]].map(([label,value])=><div key={label} className="rounded-xl border border-slate-200 bg-slate-50 p-3"><dt className="text-xs font-bold text-slate-500">{label}</dt><dd className="mt-1 text-xl font-black text-slate-950">{value}</dd></div>)}</dl><p className="mt-4 rounded-xl border border-blue-200 bg-blue-50 p-3 text-sm text-blue-900">{preview.safestNextAction}</p></section><section className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-black text-slate-950">Import pipeline</h2><p className="mt-1 text-sm text-slate-600">File contents never grant additional authority. Parsing, validation, dry-run, authorization and execution remain separate stages.</p><ol className="mt-4 grid gap-3 md:grid-cols-2">{stages.map((stage,index)=><li key={stage} className="rounded-xl border border-slate-200 p-3"><div className="text-xs font-black text-slate-500">{index+1}. {stage}</div><p className="mt-1 text-sm leading-5 text-slate-700">{importStageExperience(stage)}</p></li>)}</ol></section></div>}
