import { exportJobExperience, type ExportJobStatus } from '@/lib/phase7f/operationsExportAttachmentUx';
const statuses:ExportJobStatus[]=['QUEUED','AUTHORIZING','RUNNING','PAUSED','COMPLETED','FAILED','EXPIRED','REVOKED'];
export function ExportJobExperiencePanel(){return <section className="rounded-2xl border border-slate-200 bg-white p-5" aria-labelledby="export-job-title"><h3 id="export-job-title" className="font-black text-slate-950">Export job lifecycle</h3><p className="mt-1 text-sm text-slate-600">Access is rechecked before artifact commit. Revocation stops an in-flight job and completed artifacts are released only through secure, short-lived handles.</p><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-4">{statuses.map((status)=>{const item=exportJobExperience(status);return <article key={status} className="rounded-xl border border-slate-200 bg-slate-50 p-3"><div className="text-xs font-black uppercase tracking-wider text-slate-500">{status}</div><h4 className="mt-1 font-black text-slate-950">{item.title}</h4><p className="mt-1 text-xs leading-5 text-slate-600">{item.message}</p></article>})}</div></section>}
