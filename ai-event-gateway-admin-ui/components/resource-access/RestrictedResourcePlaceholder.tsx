export function RestrictedResourcePlaceholder({kind='resource',compact=false}:Readonly<{kind?:string;compact?:boolean}>){return <div className={`rounded-xl border border-slate-300 bg-slate-100 text-slate-700 ${compact?'px-3 py-2':'p-4'}`} role="note" aria-label={`Restricted ${kind}`}><div className="flex items-center gap-2"><span aria-hidden="true" className="grid h-7 w-7 place-items-center rounded-full bg-slate-800 text-xs text-white">🔒</span><div><div className="text-sm font-black">Restricted {kind}</div>{!compact?<p className="mt-1 text-xs leading-5 text-slate-500">This node exists in the authorized topology view, but its title, external key, URL, payload, participants, and provider identifiers are hidden.</p>:null}</div></div></div>}
