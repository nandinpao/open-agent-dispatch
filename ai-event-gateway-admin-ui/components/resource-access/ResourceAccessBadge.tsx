export function ResourceAccessBadge({value}:Readonly<{value:string}>){const normalized=(value||'UNKNOWN').toUpperCase();let cls='border-slate-200 bg-slate-50 text-slate-700';if(['ACTIVE','ALLOW','NORMAL','CONFIRMED','COMPLETED','CURRENT','CLEAN'].includes(normalized))cls='border-emerald-200 bg-emerald-50 text-emerald-800';else if(['DENY','REVOKED','QUARANTINED','DELETED','CRITICAL','EXPIRED'].includes(normalized))cls='border-rose-200 bg-rose-50 text-rose-800';else if(['DRAFT','PENDING_APPROVAL','OPEN','RESTRICTED','STALE','OWNER_MISSING','ESCALATED','HIGH'].includes(normalized))cls='border-amber-200 bg-amber-50 text-amber-800';else if(['SUMMARY','METADATA','SHADOW','SIMULATION','INTERNAL'].includes(normalized))cls='border-blue-200 bg-blue-50 text-blue-800';return <span className={`inline-flex rounded-full border px-2.5 py-1 text-xs font-black ${cls}`}>{normalized.replaceAll('_',' ')}</span>}
