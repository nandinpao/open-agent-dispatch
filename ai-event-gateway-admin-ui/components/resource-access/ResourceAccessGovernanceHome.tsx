'use client';
import { useEffect, useState } from 'react';
import { ErrorBox } from '@/components/common/ErrorBox';
import { LoadingBox } from '@/components/common/LoadingBox';
import { ResourceAccessShell } from './ResourceAccessShell';
import { GovernanceMetricCard } from './GovernanceMetricCard';
import { getGovernanceSummary, type ResourceGovernanceSummary } from '@/lib/api/domains/resourceAccessApi';
import { ResourceAccessWorkspaceGuide } from '@/components/phase7e/ResourceAccessWorkspaceGuide';

export function ResourceAccessGovernanceHome(){const [data,setData]=useState<ResourceGovernanceSummary|null>(null);const [error,setError]=useState('');useEffect(()=>{getGovernanceSummary().then(setData).catch(e=>setError(e instanceof Error?e.message:'Resource Access governance is unavailable.'))},[]);return <ResourceAccessShell title="Resource Access Governance" description="Review who can act on which resources, why access exists, which controls are expiring, and where ownership or review evidence requires attention. Backend decisions remain authoritative."><ResourceAccessWorkspaceGuide/>{error?<ErrorBox message={error}/>:!data?<LoadingBox label="Loading Resource Access governance…"/>:<><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><GovernanceMetricCard label="Active Grants" value={data.activeGrants} description="Explicit grants currently eligible for evaluation." href="/resource-access/grants" tone="blue"/><GovernanceMetricCard label="Expiring Soon" value={data.grantsExpiringSoon} description="Active grants ending within 30 days." href="/resource-access/grants?state=ACTIVE" tone="amber"/><GovernanceMetricCard label="Active Denies" value={data.activeDenies} description="Explicit denies that override all allow evidence." href="/resource-access/denies" tone="rose"/><GovernanceMetricCard label="Open Orphans" value={data.openOrphans} description="Resources that fail closed until canonical ownership is repaired." href="/resource-access/orphans" tone="rose"/><GovernanceMetricCard label="Overdue Reviews" value={data.overdueReviewItems} description="Open review items past their due date." href="/resource-access/reviews" tone="amber"/><GovernanceMetricCard label="Active Campaigns" value={data.activeReviewCampaigns} description="Access Review campaigns awaiting decisions." href="/resource-access/reviews" tone="blue"/><GovernanceMetricCard label="Shadow Mismatches" value={data.shadowMismatches24h} description="Legacy and Resource Access decisions that diverged in the last 24 hours." href="/resource-access/decision-simulator" tone="amber"/><GovernanceMetricCard label="Stale Descriptors" value={data.staleDescriptors} description="Projection records that require reconciliation or repair." href="/resource-access/orphans" tone="slate"/></section><section className="grid gap-4 lg:grid-cols-3"><GovernancePrinciple title="Permission is not scope" body="A role can grant task.read, but a Resource must still match ownership, participant, department, group, or an explicit grant."/><GovernancePrinciple title="Deny always wins" body="An effective Explicit Deny overrides grants, participants, ownership, organization scope, and Tenant role fallback."/><GovernancePrinciple title="Visibility is capped" body="An allowed read can still be reduced to metadata or summary, with sensitive fields masked by policy."/></section><p className="text-xs text-slate-500">Measured at {new Date(data.measuredAt).toLocaleString()} · Counts are Tenant-bound and protected by FORCE RLS.</p></>}</ResourceAccessShell>}
function GovernancePrinciple({title,body}:Readonly<{title:string;body:string}>){return <article className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-black text-slate-950">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{body}</p></article>}
