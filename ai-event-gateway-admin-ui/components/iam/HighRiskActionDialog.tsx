'use client';
import { useEffect,useState } from 'react';
import { ConfirmDialog } from '@/components/ui/ConfirmDialog';
export function HighRiskActionDialog({open,title,description,confirmation='CONFIRM',running=false,onCancel,onConfirm}:{open:boolean;title:string;description:string;confirmation?:string;running?:boolean;onCancel:()=>void;onConfirm:(reason:string)=>void}){const [reason,setReason]=useState('');const [phrase,setPhrase]=useState('');useEffect(()=>{if(open){setReason('');setPhrase('');}},[open]);const valid=reason.trim().length>=12&&phrase===confirmation;return <ConfirmDialog open={open} title={title} description={description} confirmLabel="Confirm action" cancelLabel="Cancel" tone="danger" isRunning={running} onCancel={onCancel} onConfirm={()=>valid&&onConfirm(reason.trim())}><div className="space-y-4"><label className="block text-sm font-bold text-slate-800">Audit reason<textarea value={reason} onChange={e=>setReason(e.target.value)} rows={4} className="mt-2 w-full rounded-xl border border-slate-300 p-3" placeholder="Explain why this high-risk change is required (minimum 12 characters)."/></label><label className="block text-sm font-bold text-slate-800">Type <code>{confirmation}</code><input value={phrase} onChange={e=>setPhrase(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 p-3 font-mono"/></label>{!valid?<p className="text-xs font-semibold text-rose-600">A complete audit reason and exact confirmation phrase are required.</p>:null}</div></ConfirmDialog>}
