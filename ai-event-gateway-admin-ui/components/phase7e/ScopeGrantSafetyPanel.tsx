'use client';
import { validateScopeGrantDraft, type ScopeGrantDraft } from '@/lib/phase7e/iamResourceAccessUx';
export function ScopeGrantSafetyPanel({ draft }: Readonly<{ draft: ScopeGrantDraft }>) { const result=validateScopeGrantDraft(draft); return <section className={`rounded-xl border p-4 text-sm ${result.status==='BLOCKED'?'border-rose-200 bg-rose-50 text-rose-950':result.status==='WARNING'?'border-amber-200 bg-amber-50 text-amber-950':'border-emerald-200 bg-emerald-50 text-emerald-950'}`} aria-live="polite"><div className="font-black">Scope Grant validation · {result.status}</div><p className="mt-1">{result.safestNextAction}</p>{result.durationHours!=null?<p className="mt-1 text-xs">Requested duration: {result.durationHours} hours</p>:null}{result.issues.length?<ul className="mt-2 list-disc space-y-1 pl-5">{result.issues.map(issue=><li key={issue}>{issue}</li>)}</ul>:null}</section> }
