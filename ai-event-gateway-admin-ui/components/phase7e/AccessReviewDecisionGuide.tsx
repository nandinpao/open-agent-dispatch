'use client';
import { accessReviewDecisionExperience, type AccessReviewDecision } from '@/lib/phase7e/iamResourceAccessUx';
const decisions:AccessReviewDecision[]=['KEEP','REDUCE','REVOKE','REQUEST_MORE_INFORMATION'];
export function AccessReviewDecisionGuide(){return <section className="rounded-2xl border border-indigo-200 bg-indigo-50/40 p-4"><h2 className="font-black text-slate-950">Review decisions</h2><p className="mt-1 text-sm text-slate-600">Every decision requires a reason. Reduce and revoke remain governed backend mutations; requesting information never extends access.</p><div className="mt-3 grid gap-3 md:grid-cols-2 xl:grid-cols-4">{decisions.map(decision=>{const item=accessReviewDecisionExperience(decision);return <article key={decision} className="rounded-xl border border-slate-200 bg-white p-3"><h3 className="font-black text-slate-900">{item.title}</h3><p className="mt-1 text-xs leading-5 text-slate-600">{item.effect}</p></article>})}</div></section>}
