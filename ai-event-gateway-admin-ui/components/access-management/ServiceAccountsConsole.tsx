/**
 * @deprecated Phase 7 compatibility export. Production routes must use the
 * canonical People / Organization / Access / Security / Tenant workspaces.
 */
export { ServiceAccountsConsole } from './legacy/ServiceAccountsConsole';
