import type{A2ATopology}from'@/lib/api/domains/a2aOperationsApi';export function A2ATopologyPanel({value}:{value:A2ATopology}){return <section className="rounded-2xl border border-indigo-200 bg-indigo-50 p-4"><p className="text-xs font-black uppercase tracking-[.16em] text-indigo-700">Authority topology</p><div className="mt-3 flex flex-wrap items-center gap-2">{value.nodes.map((n,i)=><div key={n.id} className="flex items-center gap-2"><div className="min-w-36 rounded-xl border border-indigo-200 bg-white p-3"><div className="text-xs font-black uppercase text-slate-500">{n.type} · {n.authority}</div><div className="mt-1 truncate text-sm font-black" title={n.id}>{n.label}</div><div className="mt-1 text-xs text-indigo-700">{n.status}</div></div>{i<value.nodes.length-1?<span className="font-black text-indigo-400">→</span>:null}</div>)}</div><div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-600">{value.edges.map((e,i)=><span key={`${e.source}-${e.target}-${i}`} className="rounded-full border bg-white px-3 py-1">{e.relation} · {e.status}</span>)}</div></section>}
