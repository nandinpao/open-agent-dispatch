'use client';
import { FormEvent,useState } from 'react';
import Link from 'next/link';
import { useAuth } from '@/components/auth/AuthProvider';
export default function ForgotPasswordPage(){const {forgotPassword}=useAuth();const [done,setDone]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');try{const d=new FormData(e.currentTarget);await forgotPassword(String(d.get('username')));setDone(true);}catch(x){setError(x instanceof Error?x.message:'Request failed.');}finally{setBusy(false);}}return <main className="mx-auto max-w-md pt-10"><section className="rounded-3xl border border-slate-200 bg-white p-7 shadow-sm"><p className="text-xs font-black uppercase tracking-[.18em] text-blue-600">Account recovery</p><h1 className="mt-2 text-2xl font-black">Reset your password</h1>{done?<div className="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm leading-6 text-emerald-900">If the account exists and is eligible for recovery, OpenDispatch has created a one-time reset request. Follow the approved out-of-band delivery instructions.</div>:<form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-bold">Username or email<input name="username" required className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5"/></label>{error?<div role="alert" className="rounded-xl bg-rose-50 p-3 text-sm text-rose-800">{error}</div>:null}<button disabled={busy} className="w-full rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-black text-white disabled:bg-slate-300">Request reset</button></form>}<Link href="/login" className="mt-5 inline-block text-sm font-bold text-blue-700">Back to sign in</Link></section></main>}
