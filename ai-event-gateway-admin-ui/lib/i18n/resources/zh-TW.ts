import { enMessages } from './en';

// Placeholder only. Admin UI remains English-first in Stage 7.
// Replace values incrementally when Traditional Chinese localization is approved.
export const zhTWMessages = {
  ...enMessages,
} as const;
