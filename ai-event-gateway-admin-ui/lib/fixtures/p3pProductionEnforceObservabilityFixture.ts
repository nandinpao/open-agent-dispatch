import { p3oEnforceDefaultHardeningFixture } from './p3oEnforceDefaultHardeningFixture';

export const p3pProductionEnforceObservabilityFixture = {
  id: 'P3P_PRODUCTION_ENFORCE_OBSERVABILITY',
  mode: 'ENFORCE',
  postCutoverDashboard: {
    path: '/settings/enforce-observability',
    title: 'Production ENFORCE Observability',
    liveSnapshotEndpoint: '/admin/enforce/observability',
    exportArtifact: 'p3p-post-cutover-observability.json',
  },
  metrics: {
    v2Allowed: 'opendispatch_enforce_v2_allowed_total',
    v2Blocked: 'opendispatch_enforce_v2_blocked_total',
    noCandidate: 'opendispatch_enforce_no_candidate_total',
    fallbackDenied: 'opendispatch_enforce_legacy_fallback_denied_total',
    qualityUnavailable: 'opendispatch_enforce_quality_metric_unavailable_total',
    scoreBreakdownMissing: 'opendispatch_enforce_score_breakdown_missing_total',
  },
  sampleSnapshot: {
    generatedAt: '2026-07-06T00:00:00.000Z',
    source: 'fixture',
    mode: p3oEnforceDefaultHardeningFixture.releaseProfile.enforceMode,
    window: '24h',
    v2Allowed: 128,
    v2Blocked: 17,
    noCandidate: 2,
    fallbackDenied: 0,
    qualityUnavailable: 1,
    scoreBreakdownMissing: 0,
    blockedRate: 0.117,
    noCandidateRate: 0.014,
    qualityUnavailableRate: 0.007,
    latestAcceptanceArtifact: p3oEnforceDefaultHardeningFixture.mandatoryLiveGate.acceptanceArtifact,
    latestReadinessArtifact: p3oEnforceDefaultHardeningFixture.mandatoryLiveGate.readinessArtifact,
    latestArchiveManifest: p3oEnforceDefaultHardeningFixture.archive.manifest,
    readinessBlockingCount: 0,
  },
  alertRules: [
    {
      code: 'P3P_BLOCKED_RATE_HIGH',
      metric: 'blockedRate',
      operator: '>=',
      threshold: 0.35,
      severity: 'HIGH',
      action: 'Open routing decision audit search, inspect top blocking reasons, and pause ENFORCE expansion if repeated for 15 minutes.',
    },
    {
      code: 'P3P_NO_CANDIDATE_RATE_HIGH',
      metric: 'noCandidateRate',
      operator: '>=',
      threshold: 0.1,
      severity: 'CRITICAL',
      action: 'Create operator incident and consider rollback to WARN if business-critical tasks are impacted.',
    },
    {
      code: 'P3P_QUALITY_UNAVAILABLE_RATE_HIGH',
      metric: 'qualityUnavailableRate',
      operator: '>=',
      threshold: 0.05,
      severity: 'HIGH',
      action: 'Repair quality snapshots before continuing ENFORCE operations.',
    },
    {
      code: 'P3P_FALLBACK_DENIED_NONZERO',
      metric: 'fallbackDenied',
      operator: '>',
      threshold: 0,
      severity: 'MIDDLE',
      action: 'Review legacy-only candidates and migrate missing Supply Profile / Policy / Capability data.',
    },
    {
      code: 'P3P_SCORE_BREAKDOWN_MISSING_NONZERO',
      metric: 'scoreBreakdownMissing',
      operator: '>',
      threshold: 0,
      severity: 'CRITICAL',
      action: 'Stop ENFORCE release because routing decisions are missing mandatory V2 explainability.',
    },
  ],
  routingAuditSearch: {
    endpoint: '/admin/enforce/routing-audit',
    requiredFilters: ['taskId', 'agentId', 'blockingCode', 'policyCode', 'window'],
    requiredScoreKeys: p3oEnforceDefaultHardeningFixture.enforceScoreBreakdownRequiredKeys,
  },
  operatorIncidentWorkflow: {
    endpoint: '/admin/enforce/incidents',
    issueTrackingLink: '/settings/issue-tracking',
    triggerCodes: ['P3P_NO_CANDIDATE_RATE_HIGH', 'P3P_SCORE_BREAKDOWN_MISSING_NONZERO', 'P3H_ACTIVE_RUNTIME_BINDING_REQUIRED', 'P3H_REQUIRED_CAPABILITY_MISSING', 'P3H_REQUIRED_RUNTIME_FEATURE_NOT_TRUSTED', 'P3H_QUALITY_RULE_FAILED', 'P3H_QUALITY_METRIC_UNAVAILABLE'],
    incidentTemplate: 'ENFORCE_POST_CUTOVER_BLOCKING_INCIDENT',
  },
  rollbackCriteria: [
    'P3P_NO_CANDIDATE_RATE_HIGH persists for two evaluation windows.',
    'P3P_SCORE_BREAKDOWN_MISSING_NONZERO is observed in any ENFORCE routing decision.',
    'Business-critical task queues have zero eligible V2 candidates after repair attempts.',
    'Live acceptance artifact is older than the approved retention window.',
  ],
  legacyFinalReport: {
    endpoint: '/admin/enforce/legacy-final-report',
    readonlyViews: ['p3o_legacy_readonly_report', 'p3l_legacy_feature_cleanup_report', 'p3p_legacy_final_report'],
    categories: ['CAPABILITY_TASK_COUPLED_READONLY', 'ASSIGNMENT_PROFILE_TASK_COUPLED_READONLY', 'SKILL_REGISTRY_POLICY_READONLY'],
  },
  artifactRetention: {
    endpoint: '/admin/enforce/artifact-retention',
    retentionDays: 180,
    archiveDir: p3oEnforceDefaultHardeningFixture.archive.defaultOutputDir,
    requiredArtifacts: [
      'p3n-full-enforce-summary.json',
      'p3n-enforce-runtime-acceptance.json',
      'p3n-readiness-report.json',
      'p3o-release-artifact-manifest.json',
      'p3p-post-cutover-observability.json',
    ],
  },
} as const;
