/** Canonical product routes used by contextual recovery links and cross-workspace actions. */
export const PRODUCT_ROUTES = Object.freeze({
  sourceSystems: '/source-systems',
  dispatch: '/dispatch-flows',
  agents: '/agents',
  capabilities: '/settings/capabilities',
  machineAccess: 'security?view=MACHINE',
});
