<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opensocket.aievent.core.iam.persistence.dao.IamOutboxDao">
  <insert id="insert">insert into module_outbox_events(
      outbox_id,event_id,event_type,aggregate_type,aggregate_id,payload_json,
      payload_version,tenant_id,correlation_id,actor_type,actor_id,lineage_status,
      status,attempt_count,created_at,updated_at)
    values(
      #{row.outboxId},#{row.eventId},#{row.eventType},#{row.aggregateType},#{row.aggregateId},cast(#{row.payloadJson} as jsonb),
      #{row.payloadVersion},#{row.tenantId},#{row.correlationId},#{row.actorType},#{row.actorId},#{row.lineageStatus},
      'PENDING',0,#{row.createdAt},#{row.createdAt})
    on conflict(event_id) do nothing</insert>
</mapper>
