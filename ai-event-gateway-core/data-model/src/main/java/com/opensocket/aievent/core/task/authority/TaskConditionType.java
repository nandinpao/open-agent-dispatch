package com.opensocket.aievent.core.task.authority;

/** A0-R2 canonical task conditions. */
public enum TaskConditionType {
    CAPACITY_WAIT, TRIAGE_REQUIRED, WAITING_APPROVAL, WAITING_EXTERNAL_INPUT, AUTH_REQUIRED,
    REMOTE_OUTCOME_UNKNOWN, LOCAL_OUTCOME_UNKNOWN, EVIDENCE_CONFLICT, INSUFFICIENT_EVIDENCE,
    COMPENSATING, COMPENSATION_FAILED, MANUAL_RECOVERY_REQUIRED, BUDGET_REVIEW_REQUIRED,
    RESIDENCY_BLOCKED, FLOW_CONFIGURATION_AMBIGUOUS, FINALIZATION_RETRY_PENDING,
    EVIDENCE_PERSISTENCE_BLOCKED, CASE_PERSISTENCE_BLOCKED
}
