package com.opensocket.aievent.core.dispatch;

/** Durable provenance snapshot declaring which execution authority owns a DispatchRequest. */
public enum DispatchAuthorityProvenance {
    LEGACY_COMPATIBILITY,
    A0_R7_CANONICAL
}
