package com.opensocket.aievent.core.resourceaccess.contract;

/** Stable participant subject types. Domain-specific subjects must be translated by a trusted bridge. */
public enum ResourceParticipantType { USER, DEPARTMENT, GROUP, SERVICE_ACCOUNT, AGENT_ASSIGNMENT }
