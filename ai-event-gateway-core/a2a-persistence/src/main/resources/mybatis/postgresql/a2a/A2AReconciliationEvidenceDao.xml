<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opensocket.aievent.database.persistence.a2a.dao.A2AReconciliationEvidenceDao">
 <resultMap id="Map" type="com.opensocket.aievent.database.persistence.a2a.po.A2AReconciliationEvidencePo"><id property="evidenceId" column="evidence_id"/><result property="tenantId" column="tenant_id"/><result property="caseId" column="case_id"/><result property="repairAttemptNo" column="repair_attempt_no"/><result property="eventKey" column="event_key"/><result property="evidenceType" column="evidence_type"/><result property="evidenceReference" column="evidence_reference"/><result property="evidenceHash" column="evidence_hash"/><result property="decision" column="decision"/><result property="reasonCode" column="reason_code"/><result property="safeSummary" column="safe_summary"/><result property="actorId" column="actor_id"/><result property="occurredAt" column="occurred_at"/></resultMap>
 <insert id="append">insert into a2a_reconciliation_evidence(tenant_id,evidence_id,case_id,repair_attempt_no,event_key,evidence_type,evidence_reference,evidence_hash,decision,reason_code,safe_summary,actor_id,occurred_at) values(#{value.tenantId},#{value.evidenceId},#{value.caseId},#{value.repairAttemptNo},#{value.eventKey},#{value.evidenceType},#{value.evidenceReference},#{value.evidenceHash},#{value.decision},#{value.reasonCode},#{value.safeSummary},#{value.actorId},coalesce(#{value.occurredAt},now())) on conflict(tenant_id,event_key) do nothing</insert>
 <select id="findByEvent" resultMap="Map">select * from a2a_reconciliation_evidence where tenant_id=#{tenantId} and event_key=#{eventKey}</select>
 <select id="findByCase" resultMap="Map">select * from a2a_reconciliation_evidence where tenant_id=#{tenantId} and case_id=#{caseId} order by occurred_at desc limit #{limit}</select>
</mapper>
