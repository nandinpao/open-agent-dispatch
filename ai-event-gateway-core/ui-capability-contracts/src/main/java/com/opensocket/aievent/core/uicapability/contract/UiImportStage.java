package com.opensocket.aievent.core.uicapability.contract;
/** Governed import pipeline stages. */
public enum UiImportStage { UPLOAD, PARSE, VALIDATE, DRY_RUN, AUTHORIZE, CONFLICT_PREVIEW, EXECUTE, RESULT }
