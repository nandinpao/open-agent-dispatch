package com.opensocket.aievent.core.uicapability.contract;

/** Canonical display state for navigation, fields and actions. */
public enum UiDisplayMode {
    HIDE,
    DISABLE_WITH_REASON,
    READ_ONLY,
    ENABLED,
    STEP_UP_REQUIRED,
    APPROVAL_REQUIRED,
    REQUEST_ACCESS,
    LOCKED_SECURITY,
    STALE_RELOAD
}
