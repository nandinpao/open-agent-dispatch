package com.opensocket.aievent.core.uicapability.contract;

/** Distinct result authorities shown by Phase 7C. */
public enum UiA2AResultLane {
    AGENT_RESULT,
    HUMAN_ACCEPTED_RESULT,
    EXTERNAL_ISSUE_PROJECTION
}
