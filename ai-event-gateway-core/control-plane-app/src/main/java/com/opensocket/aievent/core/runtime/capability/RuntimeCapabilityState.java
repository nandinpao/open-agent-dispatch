package com.opensocket.aievent.core.runtime.capability;

/** Public, non-sensitive rollout state exposed to the Admin UI. */
public enum RuntimeCapabilityState {
    DISABLED,
    SHADOW,
    PILOT,
    ENABLED
}
